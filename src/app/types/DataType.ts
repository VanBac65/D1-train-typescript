export type ProjectsType = {
  imgPath: string;
  name: string;
  address: string;
};

export type ServicesType = {
  icon: string;
  name: string;
};

export type ReasonType = {
  icon: string;
  name: string;
  description: string;
};

export type StatsType = {
  icon: string;
  number: number;
  name: string;
};
