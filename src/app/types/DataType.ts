export type ReasonType = {
    icon: string,
    name: string,
    description: string
}

export type ServicesType = {
    icon: string,
    name: string
}

export type StatsType = {
    icon: string,
    number: number,
    name: string
}